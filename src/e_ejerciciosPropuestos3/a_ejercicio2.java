package e_ejerciciosPropuestos3;

public class a_ejercicio2 {
	/*Evalue las expresiones
		int a=10,b=3,c=1,d,e;
		float x,y;
		
		x = a/b; // Se van a perder datos de la division(residuos)
		//c = a < b && c;// Inicialmente se declaro C como entero, y el resultado de la expresión es booleano
		d = a+b++;//Si lo que se desea es realizar a+(b+1) no se verá reflejado en la suma, pues al final de la asignación es donde se sumarira
		e = ++a-b; //Aqui se reflejara 10 (10+1-(3+1 por la anterior declaracion)) 
		y = (float)a/b; //En esa linea pues a primera vista no se nota ningun error 
	*/
	
	public static void main(String[] args) { }
}
